# deploy/terraform

Owner: Platform operations.

Boundary: Optional infrastructure-as-code for cloud resources such as managed PostgreSQL, object storage buckets, DNS, and Kubernetes clusters.

Terraform is optional and must not become required for local development.
